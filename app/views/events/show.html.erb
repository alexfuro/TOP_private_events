<h1><%= @event.description %></h1>
<p>hosted by: <%= @event.creator.username %></p>
<p><%= @event.attendences.count %> are attending:</p>
<ul>
  <% @event.attendees.pluck(:username).each do |attendee| %>
    <li><%= attendee %></li>
  <% end %>
</ul>
